@model MyAcademyCQRS.Models.ShopIndexViewModel
@{
    ViewData["Title"] = "Shop";
    Layout = "~/Views/Shared/_UILayout.cshtml";
}

<section class="page-title centred" style="background-image: url(@Url.Content("~/BageryPack/Bagery/assets/images/background/page-title.jpg"));">
    <div class="auto-container">
        <div class="content-box">
            <div class="title">
                <h1>Shop</h1>
            </div>
            <ul class="bread-crumb clearfix">
                <li><a asp-controller="Default" asp-action="Index">Home</a></li>
                <li>Shop</li>
            </ul>
        </div>
    </div>
</section>

<section class="shop-page-section shop-page-1">
    <div class="auto-container">
        <div class="row clearfix">
            <div class="col-lg-3 col-md-12 col-sm-12 sidebar-side">
                <div class="shop-sidebar default-sidebar">
                    <div class="sidebar-widget sidebar-categories">
                        <div class="widget-title">
                            <h3>Shop by Categories</h3>
                        </div>
                        <div class="widget-content">
                            <ul class="categories-list clearfix">
                                <li>
                                    <a asp-controller="Shop"
                                       asp-action="Index"
                                       class="@(Model.SelectedCategoryId is null ? "current" : string.Empty)">
                                        All Products
                                    </a>
                                </li>
                                @foreach (var category in Model.Categories)
                                {
                                    <li>
                                        <a asp-controller="Shop"
                                           asp-action="Index"
                                           asp-route-categoryId="@category.Id"
                                           class="@(Model.SelectedCategoryId == category.Id ? "current" : string.Empty)">
                                            @category.Name
                                        </a>
                                    </li>
                                }
                            </ul>
                        </div>
                    </div>
                </div>
            </div>

            <div class="col-lg-9 col-md-12 col-sm-12 content-side">
                <div class="our-shop">
                    <div class="items-sorting clearfix">
                        <div class="left-column pull-left">
                            <h6>@Model.Products.Count product listed</h6>
                        </div>
                    </div>

                    <div class="row clearfix">
                        @foreach (var item in Model.Products)
                        {
                            <div class="col-lg-4 col-md-6 col-sm-12 shop-block">
                                <div class="shop-block-one">
                                    <div class="inner-box">
                                        <figure class="image-box">
                                            <img src="@item.ImageUrl" alt="@item.Name">
                                        </figure>
                                        <div class="lower-content">
                                            <span class="price-box">$@item.Price</span>
                                            <h3><a href="#">@item.Name</a></h3>
                                            <ul class="rating clearfix">
                                                @for (var i = 0; i < Math.Min(item.Rating, 5); i++)
                                                {
                                                    <li><i class="icon-Star"></i></li>
                                                }
                                            </ul>
                                            <div class="cart-btn">
                                                <a href="#" class="theme-btn-two">Add to cart</a>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        }
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>
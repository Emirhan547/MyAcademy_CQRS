@model MyAcademyCQRS.Core.Application.Features.Results.GalleryImages.GetGalleryImageByIdQueryResult
@{
    ViewData["Title"] = "Galeri Görsel Güncelle";
}

<div class="flex-1 overflow-y-auto p-8">
    <section class="max-w-3xl bg-white dark:bg-[#251e18] rounded-lg shadow-soft border border-primary/10 p-6">
        <h1 class="text-2xl font-header font-bold text-gray-800 dark:text-white mb-6">Galeri Görsel Güncelle</h1>

        <form method="post" enctype="multipart/form-data" class="space-y-4">
            @Html.AntiForgeryToken()
            <input asp-for="Id" type="hidden" />
            <input asp-for="ImageUrl" type="hidden" />
            <input asp-for="Title" class="w-full rounded-lg" />
            <input type="file" name="File" class="w-full rounded-lg" />
            <button type="submit" class="px-4 py-2 rounded-lg bg-primary text-white text-sm font-semibold">Güncelle</button>
        </form>
    </section>
</div>